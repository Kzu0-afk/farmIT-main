# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
# For HTTPS domains in production, include scheme + domain (comma-separated)
# Example: https://yourapp.up.railway.app,https://api.yourdomain.com
CSRF_TRUSTED_ORIGINS=

# Database (optional; if unset, app uses local SQLite)
# For Supabase/Railway (Postgres), provide the full URL:
# postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=
DB_CONN_MAX_AGE=600
DB_SSL_REQUIRE=true

# Supabase (future: storage/auth integration)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

